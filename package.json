{
  "name": "drivecontrol",
  "private": true,
  "version": "1.0.0",
  "description": "DriveControl — vehicle health, service, and payments platform",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client --include=dev && npm run build --prefix client",
    "seed": "npm run seed --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
